{
  "devDependencies": {
    "@types/jest": "^29.5.0",
    "jest": "^29.4.2",
    "ts-jest": "^29.0.5"
  },
  "scripts": {
    "test": "jest"
  },
  "jest": {
    "preset": "ts-jest",
    "testEnvironment": "node",
    "testPathIgnorePatterns": [".build"]
  }
}
